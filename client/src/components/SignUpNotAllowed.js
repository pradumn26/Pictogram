import React from 'react';

const SignUpNotAllowed = () => {
    return (
        <div>
            <h2>Sign up not allowed while you are signed in</h2>
        </div>
    );
};

export default SignUpNotAllowed;